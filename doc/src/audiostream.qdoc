/*!
  \qmltype AudioStream
  \ingroup beatlib
  \brief Represents a stream reading a native audio file.

  This QML type represents an audio file being read by the operating system.
  \l AudioStream instances can only be created by the \l AudioFileSelector methods.

  This type has no public properties, signals or methods.
 */
